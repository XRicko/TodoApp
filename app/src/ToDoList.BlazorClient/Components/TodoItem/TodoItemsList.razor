@inject IApiInvoker ApiInvoker
@inject Notifier Notifier

@foreach (var todoItem in TodoItemModels)
{
    <TodoItem @key="todoItem.Id" TodoItemModel="todoItem" />
}

@code {
    [Parameter]
    public IEnumerable<TodoItemModel> TodoItemModels { get; set; } = Enumerable.Empty<TodoItemModel>();
}
